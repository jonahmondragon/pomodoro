# High priority
* Fix long break, this will also mean using a different
* Fix window resizing, the quote disappears if it's made big and then small again.
* Remove the hour digits in the timers if the time does not reach that far in
  the scope
* Maybe stop the colons from flashing when the timer is paused
* Avoid system() cancer when executing custom commands at the end of timers

# Low priority
* Either have each timer switch to a PAUSED text when paused, or have the timer
  start on a special quote before pressing space to start the timer, or both.
